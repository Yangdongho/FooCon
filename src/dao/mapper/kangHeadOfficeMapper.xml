<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.KangHeadOfficeDao">
	
	<select id="select_idLogin" parameterType="string" resultType="map">
		select * from headOffice where masternum = #{MASTER}
	</select>
	
	<select id="select_getHeadOffice" parameterType="string" resultType="map">
		select * from headOffice where masternum = #{MASTER}
	</select>
	
</mapper>




